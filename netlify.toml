[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Use Ubuntu 22.04 build image (required - Ubuntu 14.04 is no longer supported)
[build.image]
  name = "ubuntu-22.04"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

