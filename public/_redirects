# Force www - prevents duplicate content issues
https://connies-bailbonds.com/*  https://www.connies-bailbonds.com/:splat  301!

# Remove trailing slashes (except root) - prevents duplicate URLs in GSC
# Using 301! (forced) to run before SPA fallback
/centre/    /centre    301!
/about/     /about     301!
/services/  /services  301!

# SPA fallback - handles all routes
/*    /index.html   200

